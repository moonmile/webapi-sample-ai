# 画面設計書 — order-tablet

## 共通レイアウト

### ヘッダー（全画面共通）

```
┌─────────────────────────────────────────────────────────┐
│  🍣 日経寿司 タブレット                                  │
│  [ホーム] [カテゴリー] [カート] [注文状況] [レビュー投稿]   │
└─────────────────────────────────────────────────────────┘
```

- 背景色：白（`bg-white`）
- ロゴ：赤（`text-red-600`）
- ナビゲーションリンク：グレー → ホバーで赤
- 全画面の上部に固定表示

### フッター（全画面共通）

```
┌─────────────────────────────────────────┐
│  © 2024 日経寿司 | タブレットオーダーシステム │
└─────────────────────────────────────────┘
```

- 背景色：グレー（`bg-gray-800`）
- テキスト：白

---

## 画面 1：ホーム（`/`）

### 概要

来店時に最初に表示される画面。来店人数を設定して注文を開始する。

### レイアウト

```
┌─────────────────────────────────────────┐
│         （グラデーション背景: 赤→オレンジ）  │
│                                         │
│    🍣 日経寿司へようこそ                   │
│    心を込めたお寿司をお楽しみください       │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  テーブル番号: T-001               │ │
│  │                                    │ │
│  │  何名様でお越しですか？              │ │
│  │                                    │ │
│  │     [−]    4    [+]               │ │
│  │                                    │ │
│  │       [ 注文開始 →]                │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### UI 要素

| 要素 | 内容 | 備考 |
|------|------|------|
| 背景 | 赤→オレンジのグラデーション | `from-red-600 to-orange-500` |
| テーブル番号 | `T-001`（ハードコード） | 実運用では動的取得 |
| 人数セレクタ | 1〜8名、デクリメント・インクリメントボタン | デフォルト 2 |
| 注文開始ボタン | 白背景・赤テキスト | クリックで `/categories` へ遷移 |

### インタラクション

1. `[−]` ボタン押下 → guests 減少（最小 1）
2. `[+]` ボタン押下 → guests 増加（最大 8）
3. 「注文開始」押下 → sessionStorage に `guests` 保存 → `/categories` へ遷移

---

## 画面 2：カテゴリ・商品一覧（`/categories`）

### 概要

メニューカテゴリの一覧と、選択カテゴリの商品一覧を表示する。カートへの追加もこの画面で行う。

### レイアウト（カテゴリ選択ビュー）

```
┌─────────────────────────────────────────────────────────┐
│  [← 戻る]  メニューを選ぶ           🛒 カート（3）→    │
│            (赤ヘッダー)                                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  メニューカテゴリを選択してください                       │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ 握り寿司 │ │ 巻き寿司 │ │ 軍艦巻き │               │
│  │  6品     │ │  4品     │ │  4品     │               │
│  └──────────┘ └──────────┘ └──────────┘               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ 海鮮丼  │ │ 特選寿司 │ │サイドメニュー│             │
│  │  3品     │ │  4品     │ │  3品     │               │
│  └──────────┘ └──────────┘ └──────────┘               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │  汁物   │ │デザート  │ │  飲み物  │               │
│  │  2品     │ │  2品     │ │  3品     │               │
│  └──────────┘ └──────────┘ └──────────┘               │
│  ┌──────────┐                                          │
│  │ 季節限定 │                                          │
│  │  2品     │                                          │
│  └──────────┘                                          │
└─────────────────────────────────────────────────────────┘
```

### レイアウト（商品一覧ビュー）

```
┌─────────────────────────────────────────────────────────┐
│  [← 戻る]  メニューを選ぶ           🛒 カート（3）→    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  握り寿司                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [商品画像]  まぐろ                  ¥580          │   │
│  │             旨みのある赤身          [カートに追加] │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [商品画像]  サーモン                ¥480         │   │
│  │             脂のりが抜群            [カートに追加] │   │
│  └─────────────────────────────────────────────────┘   │
│  ...                                                   │
└─────────────────────────────────────────────────────────┘
```

### UI 要素

| 要素 | 内容 | 備考 |
|------|------|------|
| ヘッダー | 赤背景、戻るボタン、カートバッジ | カート数が更新リアルタイム反映 |
| カテゴリグリッド | 3カラムグリッド | カテゴリ選択でビュー切り替え |
| 商品カード | 商品名・価格・説明・追加ボタン | 画像はプレースホルダー |
| カート追加ボタン | 赤ボタン | 押すたびに数量 +1 |

### カテゴリ一覧

| ID | カテゴリ名 | 商品数 |
|----|-----------|--------|
| 1 | 握り寿司 | 6 |
| 2 | 巻き寿司 | 4 |
| 3 | 軍艦巻き | 4 |
| 4 | 海鮮丼 | 3 |
| 5 | 特選寿司 | 4 |
| 6 | サイドメニュー | 3 |
| 7 | 汁物 | 2 |
| 8 | デザート | 2 |
| 9 | 飲み物 | 3 |
| 10 | 季節限定 | 2 |

### インタラクション

1. カテゴリカード押下 → 商品一覧ビューに切り替え
2. 「カートに追加」押下 → カートに商品追加（localStorage 更新）
3. 「←戻る」押下（商品ビュー）→ カテゴリビューに戻る
4. 「🛒 カート」押下 → `/cart` へ遷移
5. API エラー時 → ダミーデータで表示継続（エラーは無視）

---

## 画面 3：カート（`/cart`）

### 概要

カート内の商品確認と数量調整を行い、注文を確定する。

### レイアウト

```
┌─────────────────────────────────────────────────────────┐
│  [← 戻る]  カートを確認する          （赤ヘッダー）      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  テーブル: T-001  |  人数: 4名                          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ まぐろ           ¥580       [−] 2 [+]   [🗑]   │   │
│  │ サーモン         ¥480       [−] 1 [+]   [🗑]   │   │
│  │ えび             ¥380       [−] 3 [+]   [🗑]   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  合計: 3品   ¥2,300                                    │
│                                                         │
│  ┌───────────────────────────────────────────────┐     │
│  │           注文を確定する →                     │     │
│  └───────────────────────────────────────────────┘     │
│                                                         │
│  （エラー時）❌ 注文に失敗しました: ...                  │
└─────────────────────────────────────────────────────────┘
```

### UI 要素

| 要素 | 内容 | 備考 |
|------|------|------|
| テーブル情報 | テーブル番号・人数 | sessionStorage から取得 |
| カートリスト | 商品名・単価・数量調整・削除 | リアルタイム小計更新 |
| 合計表示 | 品数・合計金額 | — |
| 確定ボタン | グラデーション赤ボタン | 送信中はスピナー表示 |
| エラーメッセージ | 赤背景で表示 | API エラー時のみ |

### インタラクション

1. `[−]` / `[+]` 押下 → 数量変更（localStorage 更新）
2. `[🗑]` 押下 → 商品削除（localStorage 更新）
3. 「注文を確定する」押下
   - カートアイテム数分だけ `POST /api/orders` を順次実行
   - 成功 → orderData を localStorage に保存 → `/order-complete` へ遷移
   - 失敗 → エラーメッセージ表示

---

## 画面 4：注文完了（`/order-complete`）

### 概要

注文確定後の完了画面。注文内容のサマリーと次のアクションへのナビゲーションを提供する。

### レイアウト

```
┌─────────────────────────────────────────────────────────┐
│  ご注文ありがとうございます！         （緑ヘッダー）      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ✅  ご注文が確定しました                               │
│                                                         │
│  注文番号: #4821                                        │
│  注文時刻: 2024-01-15 18:23:45                         │
│  調理時間目安: 15〜20分                                  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 注文内容                                         │   │
│  │ まぐろ × 2                             ¥1,160   │   │
│  │ サーモン × 1                            ¥480   │   │
│  │ えび × 3                              ¥1,140   │   │
│  │ ────────────────────────────────────           │   │
│  │ 合計                                  ¥2,780   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [ 注文状況を見る ]  [ 追加注文する ]  [ ホームに戻る ]  │
└─────────────────────────────────────────────────────────┘
```

### UI 要素

| 要素 | 内容 | 備考 |
|------|------|------|
| 完了アイコン | ✅ 緑チェックマーク | — |
| 注文番号 | ランダム4桁 | 画面表示用のみ（DB 値ではない） |
| 注文時刻 | 現在時刻 | — |
| 調理時間目安 | 15〜20分 | ハードコード |
| 注文内容 | localStorage の orderData から表示 | — |
| ナビゲーションボタン | 3つのアクション | それぞれ別画面へ遷移 |

---

## 画面 5：注文履歴（`/order-history`）

### 概要

セッション内の注文履歴とステータスを確認する画面。会計への導線も提供する。

### レイアウト

```
┌─────────────────────────────────────────────────────────┐
│  注文状況                               （青ヘッダー）   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  テーブル T-001 の注文状況                              │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 注文 #1234        18:20:00       🔄 調理中       │   │
│  │  まぐろ × 2  サーモン × 1                        │   │
│  │  小計: ¥1,640                                    │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 注文 #5678        18:10:00       🚚 配達中       │   │
│  │  えびフライ × 1                                  │   │
│  │  小計: ¥650                                      │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 注文 #9012        17:55:00       ✅ 完了         │   │
│  │  みそ汁 × 2                                      │   │
│  │  小計: ¥360                                      │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  セッション合計: ¥2,650                                 │
│                                                         │
│  [ お会計へ進む ]   [ 追加注文する ]                    │
└─────────────────────────────────────────────────────────┘
```

### ステータスバッジ

| ステータス値 | 表示テキスト | アイコン | 色 |
|-------------|------------|---------|-----|
| `pending` | 調理中 | 🔄 | 黄色 |
| `in_progress` | 配達中 | 🚚 | 青 |
| `completed` | 完了 | ✅ | 緑 |

### インタラクション

1. 「お会計へ進む」押下 → `/payment` へ遷移
2. 「追加注文する」押下 → `/categories` へ遷移

---

## 画面 6：支払い（`/payment`）

### 概要

支払い方法を選択して会計を確定する。

### レイアウト

```
┌─────────────────────────────────────────────────────────┐
│  お会計                                 （緑ヘッダー）   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  お支払い方法を選択してください                          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ご注文内容                                       │   │
│  │ まぐろ × 2                             ¥1,160   │   │
│  │ サーモン × 1                            ¥480   │   │
│  │ ────────────────────────────────────────────   │   │
│  │ 小計                                  ¥1,640   │   │
│  │ 消費税（10%）                           ¥164   │   │
│  │ 合計（税込）                           ¥1,804   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  支払い方法                                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │  💴 現金     │ │💳 クレジット │ │📱 モバイル  │   │
│  │  レジにて    │ │  カード      │ │  決済        │   │
│  └──────────────┘ └──────────────┘ └──────────────┘   │
│  （選択時: 枠が緑でハイライト）                          │
│                                                         │
│  ┌───────────────────────────────────────────────┐     │
│  │           支払いを確定する                      │     │
│  └───────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────┘
```

### 支払い方法

| 方法 | アイコン | サブテキスト |
|------|---------|------------|
| 現金 | 💴 | レジにてお支払いください |
| クレジットカード | 💳 | VISA / Mastercard / JCB |
| モバイル決済 | 📱 | PayPay / LINE Pay / 楽天Pay |

### 税計算

- 消費税率：10%
- 合計 = 小計 × 1.1（小数点切り捨て）

### インタラクション

1. 支払い方法ボタン押下 → 選択状態でハイライト（緑枠）
2. 「支払いを確定する」押下
   - 2秒の処理シミュレーション（スピナー表示）
   - 成功 → localStorage クリア → 完了メッセージ表示

---

## 画面 7：レビュー投稿（`/reviews`）

### 概要

食事後の評価・コメントを投稿する画面。CSRF トークンで保護されたフォーム。

### レイアウト

```
┌─────────────────────────────────────────────────────────┐
│  レビューを投稿する                   （ローズテーマ）    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  お食事はいかがでしたか？                               │
│  ぜひご感想をお聞かせください                           │
│                                                         │
│  評価                                                   │
│  ☆ ☆ ☆ ☆ ☆   → ★★★☆☆（選択後）                    │
│                                                         │
│  コメント ＊                                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ お食事のご感想をお聞かせください                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  追加情報（任意）                                       │
│  席番号: [____]  注文番号: [____]  商品番号: [____]     │
│                                                         │
│  ┌───────────────────────────────────────────────┐     │
│  │             レビューを送信する                   │     │
│  └───────────────────────────────────────────────┘     │
│                                                         │
│  （成功時）✅ レビューを送信しました！ありがとうございます│
│  （エラー時）❌ 送信に失敗しました: ...                  │
└─────────────────────────────────────────────────────────┘
```

### UI 要素

| 要素 | 内容 | 備考 |
|------|------|------|
| 星評価 | 1〜5 のボタン | 選択した星以下を色付き表示 |
| コメント | テキストエリア（必須） | 最低 1 文字 |
| 席番号 | 数値入力（任意） | `seat_id` として送信 |
| 注文番号 | 数値入力（任意） | `order_id` として送信 |
| 商品番号 | 数値入力（任意） | `product_id` として送信 |
| 送信ボタン | ローズ色ボタン | CSRF トークン取得後に活性化 |
| ステータス表示 | 成功/エラー | 送信後に表示 |

### CSRF 処理フロー

```
画面表示時
  └─ GET /sanctum/csrf-cookie
        └─ Cookie に XSRF-TOKEN がセット
              └─ フォームが活性化（isReady = true）

送信時
  └─ X-XSRF-TOKEN ヘッダーに Cookie 値をセット
        └─ POST /api/reviews
```

### インタラクション

1. 星ボタン押下 → 評価値設定（1〜5）
2. コメント入力 → フォーム値更新
3. 「レビューを送信する」押下
   - バリデーション（コメント必須）
   - CSRF トークン確認
   - `POST /api/reviews` 送信
   - 成功 → 成功メッセージ表示（フォームリセット）
   - 失敗 → エラーメッセージ表示

---

## 画面遷移図

```
        ┌─────────────────────────────────┐
        │             ヘッダーナビ          │
        │  (全画面からアクセス可能)         │
        └─────────────────────────────────┘
                       │
    ┌──────────────────┼──────────────────────────────┐
    │                  │                              │
    ▼                  ▼                              ▼
[ホーム]        [カテゴリ一覧]                  [レビュー投稿]
    │                  │
    │ 注文開始          │ カートへ
    ▼                  ▼
[カテゴリ一覧]      [カート]
                       │
                       │ 注文確定
                       ▼
                  [注文完了]
                  /    |    \
         注文状況/     |追加  \ホーム
                /      |      \
               ▼       ▼       ▼
         [注文履歴] [カテゴリ]  [ホーム]
              │
              │ お会計へ
              ▼
          [支払い]
```

---

## カラーパレット

| 用途 | Tailwind クラス | 色 |
|------|---------------|-----|
| プライマリ（注文） | `red-600`, `orange-500` | 赤・オレンジ |
| 成功・完了 | `green-500`, `green-600` | 緑 |
| 注文履歴 | `blue-500`, `blue-600` | 青 |
| レビュー | `rose-500`, `rose-600` | ローズ |
| テキスト（本文） | `gray-700`, `gray-800` | ダークグレー |
| 背景（カード） | `white`, `gray-50` | 白・ライトグレー |
| ボーダー | `gray-200`, `gray-300` | ライトグレー |
